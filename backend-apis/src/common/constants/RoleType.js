const RoleType = {
  ADMIN: "admin",
  USER: "user",
  SELLER: "seller",
};

const SupportedRoleTypes = [RoleType.ADMIN, RoleType.USER, RoleType.SELLER];

module.exports = { RoleType, SupportedRoleTypes };
