const mongoose = require("mongoose");
const { PaymentTypes } = require("../common/constants/PaymentStatus");
const { Schema } = mongoose;

const paymentSchemaType = {
  // payment against the order id from the db
  orderId: {
    type: mongoose.Types.ObjectId,
    required: true,
    ref: "orders",
  },
  // order id generated by third party payment service
  txnOrderId: {
    type: String,
    required: true,
  },
  // payment id generated by third party payment service
  txnPaymentId: {
    type: String,
  },
  txnPaymentStatus: {
    type: String,
    enum: PaymentTypes,
  },
  txnPaymentCompletedAt: {
    type: Date,
  },
  totalAmount: {
    type: Number,
    required: true,
  },
  currency: {
    type: String,
    required: true,
    default: "INR",
  },
  createdAt: {
    type: Date,
    default: Date.now(),
  },
};

const paymentSchema = new Schema(paymentSchemaType);
const Payment = mongoose.model("payment", paymentSchema);

module.exports = Payment;
